// bundle-stream by angleman, MIT
// bundle json stream entries by timestamp granularity

function BundleStream(config) {
    "use strict"
	
	var API  = {
		option: function() {
		},
		option2: function() {
		}
	}
	return API
}

module.exports = BundleStream()